# Binance Futures Trading Bot — Python Dependencies
# Tested on Python 3.11 and 3.12 (recommended)
#
# Install with:
#   pip install -r requirements.txt
#
# Windows note: python-binance depends on aiohttp (a C extension).
# Python 3.11/3.12 have pre-built wheels available — use those versions.
# Python 3.13+ alpha users need Microsoft C++ Build Tools to compile aiohttp.
# Download: https://visualstudio.microsoft.com/visual-cpp-build-tools/

# ---------------------------------------------------------------------------
# Core — exchange connectivity
# ---------------------------------------------------------------------------
# 1.0.16 avoids the heavy aiohttp/ujson/regex C-extension build requirements
# on environments without MSVC toolchain.
python-binance==1.0.16

# ---------------------------------------------------------------------------
# CLI framework
# ---------------------------------------------------------------------------
typer[all]==0.12.3

# ---------------------------------------------------------------------------
# Logging
# ---------------------------------------------------------------------------
loguru==0.7.2

# ---------------------------------------------------------------------------
# Environment / configuration
# ---------------------------------------------------------------------------
python-dotenv==1.0.1

# ---------------------------------------------------------------------------
# Type annotations support (Python 3.11 backport helpers)
# ---------------------------------------------------------------------------
typing-extensions==4.12.2

# ---------------------------------------------------------------------------
# Testing
# ---------------------------------------------------------------------------
pytest==8.2.2
pytest-cov==5.0.0
